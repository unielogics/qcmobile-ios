# Hoisted node_modules so native build tools (Gradle / NDK / ninja on Windows)
# can resolve packages via shallow paths. Without this, pnpm's
# `.pnpm/<long-hash>/node_modules/...` layout creates paths that exceed
# Windows' 260-char limit during NDK compile and gradle output write.
node-linker=hoisted
