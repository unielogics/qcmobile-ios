// Broker-facing Simulate. Reuses the self-directed simulator screen.
import { SelfDirectedSimulatorScreen } from "@/screens/simulator/SimulatorScreen";

export default function AgentSimulateRoute() {
  return <SelfDirectedSimulatorScreen />;
}
